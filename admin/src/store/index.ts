import { currentCourse, FilesToUpload, Loading, ModalOpen, User } from "store/atoms";

const Store = {
    FilesToUpload: FilesToUpload,
    User: User,
    CurrentCourse: currentCourse,
    ModalOpen: ModalOpen,
    Loading: Loading
}

export default Store